const goal = 128;
const rects = [
    [-5, 102, 49, 24],
    [-17, 100, 10, 20],
    [-25, 87, 38, 9],
    [-60, 98, 36, 23],
    [-62, 3, 10, 47],
    [-61, 53, 45, 25],
    [-59, 81, 32, 15],
    [-23, 80, 82, 5],
    [9, 5, 26, 33],
    [30, 40, 8, 10],
    [-41, 5, 22, 18],
    [-45, 33, 12, 14],
    [46, 10, 14, 30],
];

const l = Math.min(...rects.map(([x, _y, _w, _h]) => x));
const r = Math.max(...rects.map(([x, _y, w, _h]) => x + w));

const x = Math.abs(l) < Math.abs(r) ? l : r;
console.log(`(0, 0), (${x}, 0), (${x}, ${goal}), (0, ${goal})`)
