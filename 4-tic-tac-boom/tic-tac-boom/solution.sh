function move {
	if [ "$1" = " " ]; then return 0; fi
	if [ "$2" = " " ]; then return 1; fi
	if [ "$3" = " " ]; then return 2; fi
	if [ "$4" = " " ]; then return 3; fi
	if [ "$5" = " " ]; then return 4; fi
	if [ "$6" = " " ]; then return 5; fi
	if [ "$7" = " " ]; then return 6; fi
	if [ "$8" = " " ]; then return 7; fi
	if [ "$9" = " " ]; then return 8; fi
}
